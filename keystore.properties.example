# Copy to keystore.properties (git-ignored) for local signed builds.
KEYSTORE_PATH=/absolute/path/to/nekoedit-release.jks
KEYSTORE_PASSWORD=your-store-password
KEY_ALIAS=nekoedit
KEY_PASSWORD=your-key-password
